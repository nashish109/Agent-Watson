/**
 * Custom App component for Next.js.
 * Placeholder — will be used for global layout, providers, and styles.
 */

import type { AppProps } from "next/app";

function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default App;

# @agent-watson/web

Next.js web application — the primary user interface for Agent Watson.

## Development

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

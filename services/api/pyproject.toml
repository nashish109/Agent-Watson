[project]
name = "agent-watson-api"
version = "0.1.0"
description = "RESTful API gateway for Agent Watson"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "pydantic>=2.10.0",
]

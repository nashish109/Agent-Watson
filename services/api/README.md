# API Service

RESTful API gateway for Agent Watson. Built with FastAPI (Python).

Serves as the primary backend interface for the web app and CLI. Handles authentication, request routing, and orchestration across internal services.

## Development

```bash
uvicorn src.main:app --reload
```

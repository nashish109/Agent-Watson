[project]
name = "agent-watson-knowledge"
version = "0.1.0"
description = "Knowledge graph engine for Agent Watson"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.0",
    "pydantic>=2.10.0",
]

{
  "name": "@agent-watson/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration presets — ESLint, TypeScript, Prettier, and other tooling configs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

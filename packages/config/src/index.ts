/**
 * @agent-watson/config
 *
 * Shared configuration presets for TypeScript, ESLint, Prettier, and other tooling.
 * Centralizing configs ensures consistency across all monorepo packages.
 * Placeholder — configs will be added as tooling is set up.
 */

export {};

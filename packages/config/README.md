# @agent-watson/config

Shared configuration presets for tooling across the monorepo.

## Available Configs

- TypeScript (`tsconfig.base.json`)
- ESLint (flat config)
- Prettier

These presets are extended by individual packages to ensure consistent code quality.

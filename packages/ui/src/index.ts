/**
 * @agent-watson/ui
 *
 * Design system and component library for Agent Watson.
 * Contains reusable UI primitives (Button, Input, Card, Modal, etc.)
 * and composite components (KnowledgeGraph, Dashboard, Timeline, etc.).
 * Placeholder — components will be implemented in subsequent phases.
 */

export {};

{
  "name": "@agent-watson/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, utilities, constants, and schemas used across all Agent Watson packages",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
